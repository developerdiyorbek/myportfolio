export interface ChildProps {
  children: React.ReactNode;
}
export interface Props {
  title: string;
}

export interface IProject {
  id: number;
  name: string;
  codeUrl: string;
  websiteUrl: string;
  technology: string;
  image: string;
}
